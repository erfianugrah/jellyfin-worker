{
    "name": "{{ project-name }}",
    "version": "1.0.0",
    "description": "jellyfin caching",
    "main": "jellyfin.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "format": "prettier --write '**/*.{js,css,json,md}'"
    },
    "author": "{{ authors }}",
    "license": "ISC"
}